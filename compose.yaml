services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "2000:2000"
    depends_on:
      - database
    env_file:
      - .env

  database:
    image: mariadb
    ports:
      - "3306:3306"
    environment:
      MYSQL_DATABASE: overcooked
      MYSQL_USER: root
      MYSQL_PASSWORD: admin
  volumes:
    - myapp:/home/node/app